const customerMock = require('../mockData/customerMockData');

/*
describe('#getAllCustomers', function () {
    it('it should return all customers', function (done) {
        customerMock.customerMock.findAll.then(function (customers) {
            customers.should.have.be.a('object');
            customers.customer_name.should.equal('bett kip');
            done();

        }).catch(done);
    });
});*/
