const sequelizeInstance = require('../../repository/SequelizeInstance');
const sinon = require('sinon');
const chai = require('chai');
const expect = chai.expect;

describe('Sequelize Instance', () => {
    it('it should return sequelize instance', (done) => {
        done();
    });
});